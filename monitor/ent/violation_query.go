// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/akakou/ra-webs/monitor/ent/ctlog"
	"github.com/akakou/ra-webs/monitor/ent/predicate"
	"github.com/akakou/ra-webs/monitor/ent/violation"
)

// ViolationQuery is the builder for querying Violation entities.
type ViolationQuery struct {
	config
	ctx        *QueryContext
	order      []violation.OrderOption
	inters     []Interceptor
	predicates []predicate.Violation
	withCtLog  *CTLogQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ViolationQuery builder.
func (vq *ViolationQuery) Where(ps ...predicate.Violation) *ViolationQuery {
	vq.predicates = append(vq.predicates, ps...)
	return vq
}

// Limit the number of records to be returned by this query.
func (vq *ViolationQuery) Limit(limit int) *ViolationQuery {
	vq.ctx.Limit = &limit
	return vq
}

// Offset to start from.
func (vq *ViolationQuery) Offset(offset int) *ViolationQuery {
	vq.ctx.Offset = &offset
	return vq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (vq *ViolationQuery) Unique(unique bool) *ViolationQuery {
	vq.ctx.Unique = &unique
	return vq
}

// Order specifies how the records should be ordered.
func (vq *ViolationQuery) Order(o ...violation.OrderOption) *ViolationQuery {
	vq.order = append(vq.order, o...)
	return vq
}

// QueryCtLog chains the current query on the "ct_log" edge.
func (vq *ViolationQuery) QueryCtLog() *CTLogQuery {
	query := (&CTLogClient{config: vq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := vq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := vq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(violation.Table, violation.FieldID, selector),
			sqlgraph.To(ctlog.Table, ctlog.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, violation.CtLogTable, violation.CtLogColumn),
		)
		fromU = sqlgraph.SetNeighbors(vq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Violation entity from the query.
// Returns a *NotFoundError when no Violation was found.
func (vq *ViolationQuery) First(ctx context.Context) (*Violation, error) {
	nodes, err := vq.Limit(1).All(setContextOp(ctx, vq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{violation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (vq *ViolationQuery) FirstX(ctx context.Context) *Violation {
	node, err := vq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Violation ID from the query.
// Returns a *NotFoundError when no Violation ID was found.
func (vq *ViolationQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = vq.Limit(1).IDs(setContextOp(ctx, vq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{violation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (vq *ViolationQuery) FirstIDX(ctx context.Context) int {
	id, err := vq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Violation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Violation entity is found.
// Returns a *NotFoundError when no Violation entities are found.
func (vq *ViolationQuery) Only(ctx context.Context) (*Violation, error) {
	nodes, err := vq.Limit(2).All(setContextOp(ctx, vq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{violation.Label}
	default:
		return nil, &NotSingularError{violation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (vq *ViolationQuery) OnlyX(ctx context.Context) *Violation {
	node, err := vq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Violation ID in the query.
// Returns a *NotSingularError when more than one Violation ID is found.
// Returns a *NotFoundError when no entities are found.
func (vq *ViolationQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = vq.Limit(2).IDs(setContextOp(ctx, vq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{violation.Label}
	default:
		err = &NotSingularError{violation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (vq *ViolationQuery) OnlyIDX(ctx context.Context) int {
	id, err := vq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Violations.
func (vq *ViolationQuery) All(ctx context.Context) ([]*Violation, error) {
	ctx = setContextOp(ctx, vq.ctx, ent.OpQueryAll)
	if err := vq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Violation, *ViolationQuery]()
	return withInterceptors[[]*Violation](ctx, vq, qr, vq.inters)
}

// AllX is like All, but panics if an error occurs.
func (vq *ViolationQuery) AllX(ctx context.Context) []*Violation {
	nodes, err := vq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Violation IDs.
func (vq *ViolationQuery) IDs(ctx context.Context) (ids []int, err error) {
	if vq.ctx.Unique == nil && vq.path != nil {
		vq.Unique(true)
	}
	ctx = setContextOp(ctx, vq.ctx, ent.OpQueryIDs)
	if err = vq.Select(violation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (vq *ViolationQuery) IDsX(ctx context.Context) []int {
	ids, err := vq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (vq *ViolationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, vq.ctx, ent.OpQueryCount)
	if err := vq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, vq, querierCount[*ViolationQuery](), vq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (vq *ViolationQuery) CountX(ctx context.Context) int {
	count, err := vq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (vq *ViolationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, vq.ctx, ent.OpQueryExist)
	switch _, err := vq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (vq *ViolationQuery) ExistX(ctx context.Context) bool {
	exist, err := vq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ViolationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (vq *ViolationQuery) Clone() *ViolationQuery {
	if vq == nil {
		return nil
	}
	return &ViolationQuery{
		config:     vq.config,
		ctx:        vq.ctx.Clone(),
		order:      append([]violation.OrderOption{}, vq.order...),
		inters:     append([]Interceptor{}, vq.inters...),
		predicates: append([]predicate.Violation{}, vq.predicates...),
		withCtLog:  vq.withCtLog.Clone(),
		// clone intermediate query.
		sql:  vq.sql.Clone(),
		path: vq.path,
	}
}

// WithCtLog tells the query-builder to eager-load the nodes that are connected to
// the "ct_log" edge. The optional arguments are used to configure the query builder of the edge.
func (vq *ViolationQuery) WithCtLog(opts ...func(*CTLogQuery)) *ViolationQuery {
	query := (&CTLogClient{config: vq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	vq.withCtLog = query
	return vq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Violation.Query().
//		GroupBy(violation.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (vq *ViolationQuery) GroupBy(field string, fields ...string) *ViolationGroupBy {
	vq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ViolationGroupBy{build: vq}
	grbuild.flds = &vq.ctx.Fields
	grbuild.label = violation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Violation.Query().
//		Select(violation.FieldCreatedAt).
//		Scan(ctx, &v)
func (vq *ViolationQuery) Select(fields ...string) *ViolationSelect {
	vq.ctx.Fields = append(vq.ctx.Fields, fields...)
	sbuild := &ViolationSelect{ViolationQuery: vq}
	sbuild.label = violation.Label
	sbuild.flds, sbuild.scan = &vq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ViolationSelect configured with the given aggregations.
func (vq *ViolationQuery) Aggregate(fns ...AggregateFunc) *ViolationSelect {
	return vq.Select().Aggregate(fns...)
}

func (vq *ViolationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range vq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, vq); err != nil {
				return err
			}
		}
	}
	for _, f := range vq.ctx.Fields {
		if !violation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if vq.path != nil {
		prev, err := vq.path(ctx)
		if err != nil {
			return err
		}
		vq.sql = prev
	}
	return nil
}

func (vq *ViolationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Violation, error) {
	var (
		nodes       = []*Violation{}
		withFKs     = vq.withFKs
		_spec       = vq.querySpec()
		loadedTypes = [1]bool{
			vq.withCtLog != nil,
		}
	)
	if vq.withCtLog != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, violation.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Violation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Violation{config: vq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, vq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := vq.withCtLog; query != nil {
		if err := vq.loadCtLog(ctx, query, nodes, nil,
			func(n *Violation, e *CTLog) { n.Edges.CtLog = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (vq *ViolationQuery) loadCtLog(ctx context.Context, query *CTLogQuery, nodes []*Violation, init func(*Violation), assign func(*Violation, *CTLog)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Violation)
	for i := range nodes {
		if nodes[i].violation_ct_log == nil {
			continue
		}
		fk := *nodes[i].violation_ct_log
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(ctlog.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "violation_ct_log" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (vq *ViolationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := vq.querySpec()
	_spec.Node.Columns = vq.ctx.Fields
	if len(vq.ctx.Fields) > 0 {
		_spec.Unique = vq.ctx.Unique != nil && *vq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, vq.driver, _spec)
}

func (vq *ViolationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(violation.Table, violation.Columns, sqlgraph.NewFieldSpec(violation.FieldID, field.TypeInt))
	_spec.From = vq.sql
	if unique := vq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if vq.path != nil {
		_spec.Unique = true
	}
	if fields := vq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, violation.FieldID)
		for i := range fields {
			if fields[i] != violation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := vq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := vq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := vq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := vq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (vq *ViolationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(vq.driver.Dialect())
	t1 := builder.Table(violation.Table)
	columns := vq.ctx.Fields
	if len(columns) == 0 {
		columns = violation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if vq.sql != nil {
		selector = vq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if vq.ctx.Unique != nil && *vq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range vq.predicates {
		p(selector)
	}
	for _, p := range vq.order {
		p(selector)
	}
	if offset := vq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := vq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ViolationGroupBy is the group-by builder for Violation entities.
type ViolationGroupBy struct {
	selector
	build *ViolationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (vgb *ViolationGroupBy) Aggregate(fns ...AggregateFunc) *ViolationGroupBy {
	vgb.fns = append(vgb.fns, fns...)
	return vgb
}

// Scan applies the selector query and scans the result into the given value.
func (vgb *ViolationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vgb.build.ctx, ent.OpQueryGroupBy)
	if err := vgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ViolationQuery, *ViolationGroupBy](ctx, vgb.build, vgb, vgb.build.inters, v)
}

func (vgb *ViolationGroupBy) sqlScan(ctx context.Context, root *ViolationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(vgb.fns))
	for _, fn := range vgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*vgb.flds)+len(vgb.fns))
		for _, f := range *vgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*vgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ViolationSelect is the builder for selecting fields of Violation entities.
type ViolationSelect struct {
	*ViolationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (vs *ViolationSelect) Aggregate(fns ...AggregateFunc) *ViolationSelect {
	vs.fns = append(vs.fns, fns...)
	return vs
}

// Scan applies the selector query and scans the result into the given value.
func (vs *ViolationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vs.ctx, ent.OpQuerySelect)
	if err := vs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ViolationQuery, *ViolationSelect](ctx, vs.ViolationQuery, vs, vs.inters, v)
}

func (vs *ViolationSelect) sqlScan(ctx context.Context, root *ViolationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(vs.fns))
	for _, fn := range vs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*vs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
