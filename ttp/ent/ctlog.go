// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/akakou/ra_webs/ttp/ent/ctlog"
	"github.com/akakou/ra_webs/ttp/ent/tainfo"
)

// CTLog is the model entity for the CTLog schema.
type CTLog struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Domain holds the value of the "domain" field.
	Domain string `json:"domain,omitempty"`
	// PublicKey holds the value of the "public_key" field.
	PublicKey []byte `json:"public_key,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CTLogQuery when eager-loading is set.
	Edges          CTLogEdges `json:"edges"`
	ct_log_ta_info *int
	selectValues   sql.SelectValues
}

// CTLogEdges holds the relations/edges for other nodes in the graph.
type CTLogEdges struct {
	// TaInfo holds the value of the ta_info edge.
	TaInfo *TAInfo `json:"ta_info,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TaInfoOrErr returns the TaInfo value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CTLogEdges) TaInfoOrErr() (*TAInfo, error) {
	if e.loadedTypes[0] {
		if e.TaInfo == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: tainfo.Label}
		}
		return e.TaInfo, nil
	}
	return nil, &NotLoadedError{edge: "ta_info"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CTLog) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case ctlog.FieldPublicKey:
			values[i] = new([]byte)
		case ctlog.FieldID:
			values[i] = new(sql.NullInt64)
		case ctlog.FieldDomain:
			values[i] = new(sql.NullString)
		case ctlog.ForeignKeys[0]: // ct_log_ta_info
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CTLog fields.
func (cl *CTLog) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case ctlog.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			cl.ID = int(value.Int64)
		case ctlog.FieldDomain:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field domain", values[i])
			} else if value.Valid {
				cl.Domain = value.String
			}
		case ctlog.FieldPublicKey:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field public_key", values[i])
			} else if value != nil {
				cl.PublicKey = *value
			}
		case ctlog.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field ct_log_ta_info", value)
			} else if value.Valid {
				cl.ct_log_ta_info = new(int)
				*cl.ct_log_ta_info = int(value.Int64)
			}
		default:
			cl.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CTLog.
// This includes values selected through modifiers, order, etc.
func (cl *CTLog) Value(name string) (ent.Value, error) {
	return cl.selectValues.Get(name)
}

// QueryTaInfo queries the "ta_info" edge of the CTLog entity.
func (cl *CTLog) QueryTaInfo() *TAInfoQuery {
	return NewCTLogClient(cl.config).QueryTaInfo(cl)
}

// Update returns a builder for updating this CTLog.
// Note that you need to call CTLog.Unwrap() before calling this method if this CTLog
// was returned from a transaction, and the transaction was committed or rolled back.
func (cl *CTLog) Update() *CTLogUpdateOne {
	return NewCTLogClient(cl.config).UpdateOne(cl)
}

// Unwrap unwraps the CTLog entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (cl *CTLog) Unwrap() *CTLog {
	_tx, ok := cl.config.driver.(*txDriver)
	if !ok {
		panic("ent: CTLog is not a transactional entity")
	}
	cl.config.driver = _tx.drv
	return cl
}

// String implements the fmt.Stringer.
func (cl *CTLog) String() string {
	var builder strings.Builder
	builder.WriteString("CTLog(")
	builder.WriteString(fmt.Sprintf("id=%v, ", cl.ID))
	builder.WriteString("domain=")
	builder.WriteString(cl.Domain)
	builder.WriteString(", ")
	builder.WriteString("public_key=")
	builder.WriteString(fmt.Sprintf("%v", cl.PublicKey))
	builder.WriteByte(')')
	return builder.String()
}

// CTLogs is a parsable slice of CTLog.
type CTLogs []*CTLog
