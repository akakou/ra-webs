// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/akakou/ra_webs/ttp/ent/tacode"
)

// TACode is the model entity for the TACode schema.
type TACode struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// UniqueID holds the value of the "unique_id" field.
	UniqueID []byte `json:"unique_id,omitempty"`
	// CommitID holds the value of the "commit_id" field.
	CommitID string `json:"commit_id,omitempty"`
	// ActivatedAt holds the value of the "activated_at" field.
	ActivatedAt time.Time `json:"activated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TACodeQuery when eager-loading is set.
	Edges        TACodeEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TACodeEdges holds the relations/edges for other nodes in the graph.
type TACodeEdges struct {
	// Ta holds the value of the ta edge.
	Ta []*TA `json:"ta,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TaOrErr returns the Ta value or an error if the edge
// was not loaded in eager-loading.
func (e TACodeEdges) TaOrErr() ([]*TA, error) {
	if e.loadedTypes[0] {
		return e.Ta, nil
	}
	return nil, &NotLoadedError{edge: "ta"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TACode) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case tacode.FieldUniqueID:
			values[i] = new([]byte)
		case tacode.FieldID:
			values[i] = new(sql.NullInt64)
		case tacode.FieldCommitID:
			values[i] = new(sql.NullString)
		case tacode.FieldActivatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TACode fields.
func (tc *TACode) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case tacode.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			tc.ID = int(value.Int64)
		case tacode.FieldUniqueID:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field unique_id", values[i])
			} else if value != nil {
				tc.UniqueID = *value
			}
		case tacode.FieldCommitID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field commit_id", values[i])
			} else if value.Valid {
				tc.CommitID = value.String
			}
		case tacode.FieldActivatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field activated_at", values[i])
			} else if value.Valid {
				tc.ActivatedAt = value.Time
			}
		default:
			tc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TACode.
// This includes values selected through modifiers, order, etc.
func (tc *TACode) Value(name string) (ent.Value, error) {
	return tc.selectValues.Get(name)
}

// QueryTa queries the "ta" edge of the TACode entity.
func (tc *TACode) QueryTa() *TAQuery {
	return NewTACodeClient(tc.config).QueryTa(tc)
}

// Update returns a builder for updating this TACode.
// Note that you need to call TACode.Unwrap() before calling this method if this TACode
// was returned from a transaction, and the transaction was committed or rolled back.
func (tc *TACode) Update() *TACodeUpdateOne {
	return NewTACodeClient(tc.config).UpdateOne(tc)
}

// Unwrap unwraps the TACode entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tc *TACode) Unwrap() *TACode {
	_tx, ok := tc.config.driver.(*txDriver)
	if !ok {
		panic("ent: TACode is not a transactional entity")
	}
	tc.config.driver = _tx.drv
	return tc
}

// String implements the fmt.Stringer.
func (tc *TACode) String() string {
	var builder strings.Builder
	builder.WriteString("TACode(")
	builder.WriteString(fmt.Sprintf("id=%v, ", tc.ID))
	builder.WriteString("unique_id=")
	builder.WriteString(fmt.Sprintf("%v", tc.UniqueID))
	builder.WriteString(", ")
	builder.WriteString("commit_id=")
	builder.WriteString(tc.CommitID)
	builder.WriteString(", ")
	builder.WriteString("activated_at=")
	builder.WriteString(tc.ActivatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// TACodes is a parsable slice of TACode.
type TACodes []*TACode
