// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/akakou/ra_webs/ttp/ent/ctaudit"
)

// CTAudit is the model entity for the CTAudit schema.
type CTAudit struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// IsValid holds the value of the "is_valid" field.
	IsValid bool `json:"is_valid,omitempty"`
	// Last holds the value of the "last" field.
	Last string `json:"last,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CTAuditQuery when eager-loading is set.
	Edges        CTAuditEdges `json:"edges"`
	selectValues sql.SelectValues
}

// CTAuditEdges holds the relations/edges for other nodes in the graph.
type CTAuditEdges struct {
	// Ta holds the value of the ta edge.
	Ta []*TA `json:"ta,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TaOrErr returns the Ta value or an error if the edge
// was not loaded in eager-loading.
func (e CTAuditEdges) TaOrErr() ([]*TA, error) {
	if e.loadedTypes[0] {
		return e.Ta, nil
	}
	return nil, &NotLoadedError{edge: "ta"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CTAudit) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case ctaudit.FieldIsValid:
			values[i] = new(sql.NullBool)
		case ctaudit.FieldID:
			values[i] = new(sql.NullInt64)
		case ctaudit.FieldLast:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CTAudit fields.
func (ca *CTAudit) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case ctaudit.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ca.ID = int(value.Int64)
		case ctaudit.FieldIsValid:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_valid", values[i])
			} else if value.Valid {
				ca.IsValid = value.Bool
			}
		case ctaudit.FieldLast:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field last", values[i])
			} else if value.Valid {
				ca.Last = value.String
			}
		default:
			ca.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CTAudit.
// This includes values selected through modifiers, order, etc.
func (ca *CTAudit) Value(name string) (ent.Value, error) {
	return ca.selectValues.Get(name)
}

// QueryTa queries the "ta" edge of the CTAudit entity.
func (ca *CTAudit) QueryTa() *TAQuery {
	return NewCTAuditClient(ca.config).QueryTa(ca)
}

// Update returns a builder for updating this CTAudit.
// Note that you need to call CTAudit.Unwrap() before calling this method if this CTAudit
// was returned from a transaction, and the transaction was committed or rolled back.
func (ca *CTAudit) Update() *CTAuditUpdateOne {
	return NewCTAuditClient(ca.config).UpdateOne(ca)
}

// Unwrap unwraps the CTAudit entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ca *CTAudit) Unwrap() *CTAudit {
	_tx, ok := ca.config.driver.(*txDriver)
	if !ok {
		panic("ent: CTAudit is not a transactional entity")
	}
	ca.config.driver = _tx.drv
	return ca
}

// String implements the fmt.Stringer.
func (ca *CTAudit) String() string {
	var builder strings.Builder
	builder.WriteString("CTAudit(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ca.ID))
	builder.WriteString("is_valid=")
	builder.WriteString(fmt.Sprintf("%v", ca.IsValid))
	builder.WriteString(", ")
	builder.WriteString("last=")
	builder.WriteString(ca.Last)
	builder.WriteByte(')')
	return builder.String()
}

// CTAudits is a parsable slice of CTAudit.
type CTAudits []*CTAudit
