// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/akakou/ra_webs/ttp/ent/ta"
	"github.com/akakou/ra_webs/ttp/ent/taauditlog"
)

// TAAuditLog is the model entity for the TAAuditLog schema.
type TAAuditLog struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// IsValid holds the value of the "is_valid" field.
	IsValid bool `json:"is_valid,omitempty"`
	// LatestCtID holds the value of the "latest_ct_id" field.
	LatestCtID string `json:"latest_ct_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TAAuditLogQuery when eager-loading is set.
	Edges        TAAuditLogEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TAAuditLogEdges holds the relations/edges for other nodes in the graph.
type TAAuditLogEdges struct {
	// Ta holds the value of the ta edge.
	Ta *TA `json:"ta,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TaOrErr returns the Ta value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TAAuditLogEdges) TaOrErr() (*TA, error) {
	if e.loadedTypes[0] {
		if e.Ta == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: ta.Label}
		}
		return e.Ta, nil
	}
	return nil, &NotLoadedError{edge: "ta"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TAAuditLog) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case taauditlog.FieldIsValid:
			values[i] = new(sql.NullBool)
		case taauditlog.FieldID:
			values[i] = new(sql.NullInt64)
		case taauditlog.FieldLatestCtID:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TAAuditLog fields.
func (tal *TAAuditLog) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case taauditlog.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			tal.ID = int(value.Int64)
		case taauditlog.FieldIsValid:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_valid", values[i])
			} else if value.Valid {
				tal.IsValid = value.Bool
			}
		case taauditlog.FieldLatestCtID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field latest_ct_id", values[i])
			} else if value.Valid {
				tal.LatestCtID = value.String
			}
		default:
			tal.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TAAuditLog.
// This includes values selected through modifiers, order, etc.
func (tal *TAAuditLog) Value(name string) (ent.Value, error) {
	return tal.selectValues.Get(name)
}

// QueryTa queries the "ta" edge of the TAAuditLog entity.
func (tal *TAAuditLog) QueryTa() *TAQuery {
	return NewTAAuditLogClient(tal.config).QueryTa(tal)
}

// Update returns a builder for updating this TAAuditLog.
// Note that you need to call TAAuditLog.Unwrap() before calling this method if this TAAuditLog
// was returned from a transaction, and the transaction was committed or rolled back.
func (tal *TAAuditLog) Update() *TAAuditLogUpdateOne {
	return NewTAAuditLogClient(tal.config).UpdateOne(tal)
}

// Unwrap unwraps the TAAuditLog entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tal *TAAuditLog) Unwrap() *TAAuditLog {
	_tx, ok := tal.config.driver.(*txDriver)
	if !ok {
		panic("ent: TAAuditLog is not a transactional entity")
	}
	tal.config.driver = _tx.drv
	return tal
}

// String implements the fmt.Stringer.
func (tal *TAAuditLog) String() string {
	var builder strings.Builder
	builder.WriteString("TAAuditLog(")
	builder.WriteString(fmt.Sprintf("id=%v, ", tal.ID))
	builder.WriteString("is_valid=")
	builder.WriteString(fmt.Sprintf("%v", tal.IsValid))
	builder.WriteString(", ")
	builder.WriteString("latest_ct_id=")
	builder.WriteString(tal.LatestCtID)
	builder.WriteByte(')')
	return builder.String()
}

// TAAuditLogs is a parsable slice of TAAuditLog.
type TAAuditLogs []*TAAuditLog
