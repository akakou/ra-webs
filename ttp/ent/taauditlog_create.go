// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/akakou/ra_webs/ttp/ent/ta"
	"github.com/akakou/ra_webs/ttp/ent/taauditlog"
)

// TAAuditLogCreate is the builder for creating a TAAuditLog entity.
type TAAuditLogCreate struct {
	config
	mutation *TAAuditLogMutation
	hooks    []Hook
}

// SetIsValid sets the "is_valid" field.
func (talc *TAAuditLogCreate) SetIsValid(b bool) *TAAuditLogCreate {
	talc.mutation.SetIsValid(b)
	return talc
}

// SetLatestCtID sets the "latest_ct_id" field.
func (talc *TAAuditLogCreate) SetLatestCtID(s string) *TAAuditLogCreate {
	talc.mutation.SetLatestCtID(s)
	return talc
}

// SetTaID sets the "ta" edge to the TA entity by ID.
func (talc *TAAuditLogCreate) SetTaID(id int) *TAAuditLogCreate {
	talc.mutation.SetTaID(id)
	return talc
}

// SetNillableTaID sets the "ta" edge to the TA entity by ID if the given value is not nil.
func (talc *TAAuditLogCreate) SetNillableTaID(id *int) *TAAuditLogCreate {
	if id != nil {
		talc = talc.SetTaID(*id)
	}
	return talc
}

// SetTa sets the "ta" edge to the TA entity.
func (talc *TAAuditLogCreate) SetTa(t *TA) *TAAuditLogCreate {
	return talc.SetTaID(t.ID)
}

// Mutation returns the TAAuditLogMutation object of the builder.
func (talc *TAAuditLogCreate) Mutation() *TAAuditLogMutation {
	return talc.mutation
}

// Save creates the TAAuditLog in the database.
func (talc *TAAuditLogCreate) Save(ctx context.Context) (*TAAuditLog, error) {
	return withHooks(ctx, talc.sqlSave, talc.mutation, talc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (talc *TAAuditLogCreate) SaveX(ctx context.Context) *TAAuditLog {
	v, err := talc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (talc *TAAuditLogCreate) Exec(ctx context.Context) error {
	_, err := talc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (talc *TAAuditLogCreate) ExecX(ctx context.Context) {
	if err := talc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (talc *TAAuditLogCreate) check() error {
	if _, ok := talc.mutation.IsValid(); !ok {
		return &ValidationError{Name: "is_valid", err: errors.New(`ent: missing required field "TAAuditLog.is_valid"`)}
	}
	if _, ok := talc.mutation.LatestCtID(); !ok {
		return &ValidationError{Name: "latest_ct_id", err: errors.New(`ent: missing required field "TAAuditLog.latest_ct_id"`)}
	}
	return nil
}

func (talc *TAAuditLogCreate) sqlSave(ctx context.Context) (*TAAuditLog, error) {
	if err := talc.check(); err != nil {
		return nil, err
	}
	_node, _spec := talc.createSpec()
	if err := sqlgraph.CreateNode(ctx, talc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	talc.mutation.id = &_node.ID
	talc.mutation.done = true
	return _node, nil
}

func (talc *TAAuditLogCreate) createSpec() (*TAAuditLog, *sqlgraph.CreateSpec) {
	var (
		_node = &TAAuditLog{config: talc.config}
		_spec = sqlgraph.NewCreateSpec(taauditlog.Table, sqlgraph.NewFieldSpec(taauditlog.FieldID, field.TypeInt))
	)
	if value, ok := talc.mutation.IsValid(); ok {
		_spec.SetField(taauditlog.FieldIsValid, field.TypeBool, value)
		_node.IsValid = value
	}
	if value, ok := talc.mutation.LatestCtID(); ok {
		_spec.SetField(taauditlog.FieldLatestCtID, field.TypeString, value)
		_node.LatestCtID = value
	}
	if nodes := talc.mutation.TaIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2O,
			Inverse: false,
			Table:   taauditlog.TaTable,
			Columns: []string{taauditlog.TaColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(ta.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// TAAuditLogCreateBulk is the builder for creating many TAAuditLog entities in bulk.
type TAAuditLogCreateBulk struct {
	config
	err      error
	builders []*TAAuditLogCreate
}

// Save creates the TAAuditLog entities in the database.
func (talcb *TAAuditLogCreateBulk) Save(ctx context.Context) ([]*TAAuditLog, error) {
	if talcb.err != nil {
		return nil, talcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(talcb.builders))
	nodes := make([]*TAAuditLog, len(talcb.builders))
	mutators := make([]Mutator, len(talcb.builders))
	for i := range talcb.builders {
		func(i int, root context.Context) {
			builder := talcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*TAAuditLogMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, talcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, talcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, talcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (talcb *TAAuditLogCreateBulk) SaveX(ctx context.Context) []*TAAuditLog {
	v, err := talcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (talcb *TAAuditLogCreateBulk) Exec(ctx context.Context) error {
	_, err := talcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (talcb *TAAuditLogCreateBulk) ExecX(ctx context.Context) {
	if err := talcb.Exec(ctx); err != nil {
		panic(err)
	}
}
