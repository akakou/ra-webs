// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/akakou/ra_webs/ttp/ent/tacode"
	"github.com/akakou/ra_webs/ttp/ent/tainfo"
)

// TACodeCreate is the builder for creating a TACode entity.
type TACodeCreate struct {
	config
	mutation *TACodeMutation
	hooks    []Hook
}

// SetProductID sets the "product_id" field.
func (tcc *TACodeCreate) SetProductID(u uint16) *TACodeCreate {
	tcc.mutation.SetProductID(u)
	return tcc
}

// SetCommitID sets the "commit_id" field.
func (tcc *TACodeCreate) SetCommitID(s string) *TACodeCreate {
	tcc.mutation.SetCommitID(s)
	return tcc
}

// AddTaInfoIDs adds the "ta_info" edge to the TAInfo entity by IDs.
func (tcc *TACodeCreate) AddTaInfoIDs(ids ...int) *TACodeCreate {
	tcc.mutation.AddTaInfoIDs(ids...)
	return tcc
}

// AddTaInfo adds the "ta_info" edges to the TAInfo entity.
func (tcc *TACodeCreate) AddTaInfo(t ...*TAInfo) *TACodeCreate {
	ids := make([]int, len(t))
	for i := range t {
		ids[i] = t[i].ID
	}
	return tcc.AddTaInfoIDs(ids...)
}

// Mutation returns the TACodeMutation object of the builder.
func (tcc *TACodeCreate) Mutation() *TACodeMutation {
	return tcc.mutation
}

// Save creates the TACode in the database.
func (tcc *TACodeCreate) Save(ctx context.Context) (*TACode, error) {
	return withHooks(ctx, tcc.sqlSave, tcc.mutation, tcc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (tcc *TACodeCreate) SaveX(ctx context.Context) *TACode {
	v, err := tcc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (tcc *TACodeCreate) Exec(ctx context.Context) error {
	_, err := tcc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tcc *TACodeCreate) ExecX(ctx context.Context) {
	if err := tcc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (tcc *TACodeCreate) check() error {
	if _, ok := tcc.mutation.ProductID(); !ok {
		return &ValidationError{Name: "product_id", err: errors.New(`ent: missing required field "TACode.product_id"`)}
	}
	if _, ok := tcc.mutation.CommitID(); !ok {
		return &ValidationError{Name: "commit_id", err: errors.New(`ent: missing required field "TACode.commit_id"`)}
	}
	return nil
}

func (tcc *TACodeCreate) sqlSave(ctx context.Context) (*TACode, error) {
	if err := tcc.check(); err != nil {
		return nil, err
	}
	_node, _spec := tcc.createSpec()
	if err := sqlgraph.CreateNode(ctx, tcc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	tcc.mutation.id = &_node.ID
	tcc.mutation.done = true
	return _node, nil
}

func (tcc *TACodeCreate) createSpec() (*TACode, *sqlgraph.CreateSpec) {
	var (
		_node = &TACode{config: tcc.config}
		_spec = sqlgraph.NewCreateSpec(tacode.Table, sqlgraph.NewFieldSpec(tacode.FieldID, field.TypeInt))
	)
	if value, ok := tcc.mutation.ProductID(); ok {
		_spec.SetField(tacode.FieldProductID, field.TypeUint16, value)
		_node.ProductID = value
	}
	if value, ok := tcc.mutation.CommitID(); ok {
		_spec.SetField(tacode.FieldCommitID, field.TypeString, value)
		_node.CommitID = value
	}
	if nodes := tcc.mutation.TaInfoIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2M,
			Inverse: false,
			Table:   tacode.TaInfoTable,
			Columns: tacode.TaInfoPrimaryKey,
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(tainfo.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// TACodeCreateBulk is the builder for creating many TACode entities in bulk.
type TACodeCreateBulk struct {
	config
	err      error
	builders []*TACodeCreate
}

// Save creates the TACode entities in the database.
func (tccb *TACodeCreateBulk) Save(ctx context.Context) ([]*TACode, error) {
	if tccb.err != nil {
		return nil, tccb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(tccb.builders))
	nodes := make([]*TACode, len(tccb.builders))
	mutators := make([]Mutator, len(tccb.builders))
	for i := range tccb.builders {
		func(i int, root context.Context) {
			builder := tccb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*TACodeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, tccb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, tccb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, tccb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (tccb *TACodeCreateBulk) SaveX(ctx context.Context) []*TACode {
	v, err := tccb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (tccb *TACodeCreateBulk) Exec(ctx context.Context) error {
	_, err := tccb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tccb *TACodeCreateBulk) ExecX(ctx context.Context) {
	if err := tccb.Exec(ctx); err != nil {
		panic(err)
	}
}
